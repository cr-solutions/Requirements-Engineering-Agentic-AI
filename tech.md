# Tech Stack: [Your Project Name]

## Language & Runtime

- **[Language + Version]** (e.g., Python 3.12, Node 20 LTS)
- Virtual environment at `.venv` (per-repo)

## Dependencies

| Package | Version | Purpose |
|---------|---------|---------|
| `[package-name]` | X.Y.Z | [What it does in your project] |
| `[package-name]` | X.Y.Z | [What it does in your project] |

## Build System

[Describe how the project is built — e.g., setuptools, webpack, cargo, etc.]

### Common Commands

```bash
# Install dependencies
[command]

# Run the project
[command]

# Build for production
[command]
```

## Testing

- **Framework:** [e.g., pytest, jest, unittest]
- **Test location:** `test/` or `tests/`
- **Run tests:** `[command]`
- **Coverage target:** [e.g., 100%, 80%]
- **Coverage report:** `[output location]`

## Environment Variables

| Variable | Default | Purpose |
|----------|---------|---------|
| `[VAR_NAME]` | `[default]` | [What it configures] |
| `[VAR_NAME]` | `[default]` | [What it configures] |

## Deployment

- **Target environment:** [e.g., Linux (WSL compatible), Docker, AWS Lambda]
- **CI/CD:** [e.g., GitHub Actions, Jenkins, none]
